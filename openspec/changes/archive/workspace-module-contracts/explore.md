# Exploration: GitHub-only MVP

Team lead wants module folders. Product MVP is now GitHub only.

## Quick path

1. Four modules: `convex`, `apps/mobile`, `packages/domain`, `packages/github`
2. Each has `CONTRACT.md`
3. No YouTube/Luma/Zernio in this change

## Pull check

`main` at `437a30e` is already up to date. Skills on disk remain Convex-only under `.agents/skills/`. No new skills arrived.

## Next

Contracts in the four folders; other connector stubs removed so agents do not implement them.
